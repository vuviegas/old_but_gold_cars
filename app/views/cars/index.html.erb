<div class="container">
  <div class="d-flex row justify-content-start">
    <% @cars.each do |car| %>
      <div class="card-product mr-5">
        <% if car.photos.attached?  %>
          <%= cl_image_tag car.photos.first.key, height: 300, width: 400, crop: :fill, class: "picture-car-index" %>
        <% else %>
           <%= image_tag "old_car_icon", class: "picture-car-index default-car" %>
        <% end %>
          <div class="card-product-infos">
          <h2><%= car.model %> | <%= car.year %></h2>
          <h3 class="card-car-pricing">R$ <%= car.price %></h3>
          <%= link_to car_path(car), :class => "btn btn-info btn-sm" do %>detalhes<% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
